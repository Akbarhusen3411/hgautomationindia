# Production Environment Variables
# Disable source maps to prevent code exposure
GENERATE_SOURCEMAP=false

# Inline runtime chunk for better security
INLINE_RUNTIME_CHUNK=true

# Build for production
NODE_ENV=production
